name = "lumina-collection-api"
main = "src/index.ts"
compatibility_date = "2026-09-15"
workers_dev = true

# Before deployment, bind a Workers KV namespace to this Worker with the
# binding name LUMINA_KV. The production VertexCraft worker can also merge
# these /v1 routes and expose the same binding.
