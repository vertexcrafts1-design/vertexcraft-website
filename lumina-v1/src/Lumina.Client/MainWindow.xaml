<Window x:Class="Lumina.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="LUMINA" Width="1320" Height="820" MinWidth="1080" MinHeight="680"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        Background="{StaticResource Bg}" Foreground="{StaticResource PrimaryText}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0"/>
    </shell:WindowChrome.WindowChrome>

    <Grid Background="{StaticResource Bg}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="224"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource Sidebar}" BorderBrush="#1D2230" BorderThickness="0,0,1,0">
            <Grid Margin="14,14,14,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="58"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                    <Border Width="38" Height="38" CornerRadius="11">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#A78BFA" Offset="0"/>
                                <GradientStop Color="#6D5DFB" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="L" FontSize="21" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="LUMINA" FontSize="17" FontWeight="Bold"/>
                        <TextBlock Text="Minecraft Client" FontSize="10" Foreground="{StaticResource MutedText}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel x:Name="SidebarNav" Grid.Row="1" Margin="0,18,0,0">
                    <TextBlock Text="CLIENT" FontSize="10" FontWeight="SemiBold" Foreground="#596176" Margin="14,0,0,7"/>
                    <Button Style="{StaticResource NavButton}" Tag="Home" Click="Nav_Click" Background="#1A2030" Foreground="White" Content="⌂   Home"/>
                    <Button Style="{StaticResource NavButton}" Tag="Instances" Click="Nav_Click" Content="▦   Instanzen"/>
                    <Button Style="{StaticResource NavButton}" Tag="Library" Click="Nav_Click" Content="◇   Bibliothek"/>
                    <Button Style="{StaticResource NavButton}" Tag="Lab" Click="Nav_Click" Content="✦   LUMINA Lab"/>
                    <Button Style="{StaticResource NavButton}" Tag="Downloads" Click="Nav_Click" Content="↓   Downloads"/>
                    <TextBlock Text="SYSTEM" FontSize="10" FontWeight="SemiBold" Foreground="#596176" Margin="14,24,0,7"/>
                    <Button Style="{StaticResource NavButton}" Tag="Settings" Click="Nav_Click" Content="⚙   Einstellungen"/>
                    <Button Style="{StaticResource NavButton}" Tag="Account" Click="Nav_Click" Content="◎   Account"/>
                </StackPanel>

                <Button Grid.Row="2" Style="{StaticResource NavButton}" Tag="Account" Click="Nav_Click" Padding="12,10">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="34" Height="34" CornerRadius="17" Background="#242B3D">
                            <TextBlock Text="☺" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="AccountMiniName" Text="Nicht angemeldet" FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock Text="Microsoft Account" FontSize="10" Foreground="{StaticResource MutedText}"/>
                        </StackPanel>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Main -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" Background="#0A0D14" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="PageTitle" Text="Home" FontSize="13" Foreground="#727C92" VerticalAlignment="Center" Margin="26,0,0,0"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Width="46" Height="48" Background="Transparent" BorderThickness="0" Foreground="#8992A6" Click="Minimize_Click" Content="—"/>
                    <Button Width="46" Height="48" Background="Transparent" BorderThickness="0" Foreground="#8992A6" Click="Maximize_Click" Content="□"/>
                    <Button Width="46" Height="48" Background="Transparent" BorderThickness="0" Foreground="#8992A6" Click="Close_Click" Content="×" FontSize="18"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="28,18,28,28">
                <!-- HOME -->
                <ScrollViewer x:Name="HomePage" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Willkommen zurück" FontSize="29" FontWeight="Bold"/>
                        <TextBlock Text="Dein Minecraft. Deine Instanzen. Dein Client." FontSize="14" Foreground="{StaticResource SecondaryText}" Margin="0,5,0,22"/>

                        <Border CornerRadius="22" Padding="34" MinHeight="245" ClipToBounds="True">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#35255D" Offset="0"/>
                                    <GradientStop Color="#191B35" Offset="0.52"/>
                                    <GradientStop Color="#111521" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <Border Background="#2AFFFFFF" CornerRadius="8" Padding="10,5" HorizontalAlignment="Left">
                                        <TextBlock Text="AKTIVE INSTANZ" FontSize="10" FontWeight="Bold" Foreground="#D8CCFF"/>
                                    </Border>
                                    <TextBlock x:Name="HeroInstanceName" Text="LUMINA Vanilla" FontSize="34" FontWeight="Bold" Margin="0,13,0,3"/>
                                    <TextBlock x:Name="HeroInstanceMeta" Text="Minecraft 1.21.1 • Vanilla" FontSize="14" Foreground="#B8BDD0"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,22,0,0">
                                        <Button x:Name="HomePlayButton" Style="{StaticResource PrimaryButton}" Click="Play_Click" Padding="28,13" Content="▶  SPIELEN"/>
                                        <Button Style="{StaticResource SecondaryButton}" Click="OpenInstanceFolder_Click" Margin="10,0,0,0" Content="Ordner öffnen"/>
                                    </StackPanel>
                                    <TextBlock x:Name="HomeStatus" Text="Bereit" FontSize="11" Foreground="#959EB4" Margin="0,12,0,0"/>
                                </StackPanel>
                                <Border Grid.Column="1" Background="#25111724" BorderBrush="#36FFFFFF" BorderThickness="1" CornerRadius="18" Padding="22" Margin="18,0,0,0">
                                    <StackPanel>
                                        <TextBlock Text="LUMINA PROFILE" FontSize="10" FontWeight="Bold" Foreground="#8D96AD"/>
                                        <TextBlock x:Name="HeroPreset" Text="Balanced" FontSize="25" FontWeight="Bold" Margin="0,7,0,2"/>
                                        <TextBlock Text="Performance-Profil dieser Instanz" FontSize="11" Foreground="#8D96AD" TextWrapping="Wrap"/>
                                        <Border Height="1" Background="#2D3548" Margin="0,17"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Safe Launch" Foreground="#AAB2C5"/>
                                            <TextBlock x:Name="HeroSafeLaunch" Text="  AKTIV" Foreground="#9AE6B4" FontWeight="Bold"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
                                            <TextBlock Text="Content" Foreground="#AAB2C5"/>
                                            <TextBlock x:Name="HeroContentMini" Text="  0 Dateien" Foreground="#D9DDF0" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                        <UniformGrid Columns="3" Margin="0,16,0,0">
                            <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="0,0,8,0">
                                <StackPanel><TextBlock Text="STARTS" FontSize="10" Foreground="{StaticResource MutedText}" FontWeight="Bold"/><TextBlock x:Name="HomeLaunchCount" Text="0" FontSize="25" FontWeight="Bold" Margin="0,4,0,0"/></StackPanel>
                            </Border>
                            <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="4,0">
                                <StackPanel><TextBlock Text="SPIELZEIT" FontSize="10" Foreground="{StaticResource MutedText}" FontWeight="Bold"/><TextBlock x:Name="HomePlaytime" Text="0 h" FontSize="25" FontWeight="Bold" Margin="0,4,0,0"/></StackPanel>
                            </Border>
                            <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="14" Padding="18" Margin="8,0,0,0">
                                <StackPanel><TextBlock Text="CONTENT" FontSize="10" Foreground="{StaticResource MutedText}" FontWeight="Bold"/><TextBlock x:Name="HomeContentCount" Text="0" FontSize="25" FontWeight="Bold" Margin="0,4,0,0"/></StackPanel>
                            </Border>
                        </UniformGrid>

                        <Grid Margin="0,24,0,8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Quick Profiles" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="Ein Klick ändert das Performance-Profil der aktiven Instanz." FontSize="11" Foreground="{StaticResource SecondaryText}" Margin="0,3,0,0"/></StackPanel>
                        </Grid>
                        <UniformGrid Columns="4">
                            <Button Style="{StaticResource SecondaryButton}" Tag="Competitive" Click="Preset_Click" Margin="0,0,6,0" Padding="12,14" Content="⚡ Competitive"/>
                            <Button Style="{StaticResource SecondaryButton}" Tag="Balanced" Click="Preset_Click" Margin="2,0,4,0" Padding="12,14" Content="◈ Balanced"/>
                            <Button Style="{StaticResource SecondaryButton}" Tag="Quality" Click="Preset_Click" Margin="4,0,2,0" Padding="12,14" Content="✦ Quality"/>
                            <Button Style="{StaticResource SecondaryButton}" Tag="Low Spec" Click="Preset_Click" Margin="6,0,0,0" Padding="12,14" Content="◌ Low Spec"/>
                        </UniformGrid>

                        <TextBlock Text="Letzte Sessions" FontSize="18" FontWeight="SemiBold" Margin="0,24,0,10"/>
                        <ListBox x:Name="RecentList" Background="Transparent" BorderThickness="0" Foreground="White" IsHitTestVisible="False"/>
                    </StackPanel>
                </ScrollViewer>

                <!-- INSTANCES -->
                <Grid x:Name="InstancesPage" Visibility="Collapsed">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <Grid Margin="0,0,0,18">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Instanzen" FontSize="29" FontWeight="Bold"/><TextBlock Text="Jede Instanz hat eigene Mods, Packs, Einstellungen und Dateien." FontSize="13" Foreground="{StaticResource SecondaryText}" Margin="0,4,0,0"/></StackPanel>
                        <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Click="NewInstance_Click" Content="＋ Neue Instanz"/>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="390"/><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="10">
                            <ListBox x:Name="InstancesList" Background="Transparent" BorderThickness="0" SelectionChanged="InstancesList_SelectionChanged" Foreground="White">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,3"/><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/>
                                        <Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#242B40"/></Trigger></Style.Triggers>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="10,10">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="44"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                            <Border Width="38" Height="38" CornerRadius="10" Background="#2D2450"><TextBlock Text="L" FontWeight="Bold" Foreground="#C7B9FF" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                            <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                                <TextBlock Text="{Binding Subtitle}" Foreground="#7D879C" FontSize="10" Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Border>
                        <StackPanel Grid.Column="2">
                            <Border x:Name="NewInstancePanel" Background="#161B28" BorderBrush="#40356A" BorderThickness="1" CornerRadius="16" Padding="20" Visibility="Collapsed" Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="Neue Instanz" FontSize="17" FontWeight="SemiBold"/>
                                    <Grid Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="Name" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/><TextBox x:Name="NewInstanceName" Text="Neue Instanz"/></StackPanel>
                                        <StackPanel Grid.Column="2"><TextBlock Text="Minecraft Version" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,0,0,5"/><TextBox x:Name="NewInstanceVersion" Text="1.21.1"/></StackPanel>
                                    </Grid>
                                    <TextBlock Text="Loader" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,12,0,5"/>
                                    <ComboBox x:Name="NewInstanceLoader" SelectedIndex="1"><ComboBoxItem Content="Vanilla"/><ComboBoxItem Content="Fabric"/><ComboBoxItem Content="Quilt"/></ComboBox>
                                    <StackPanel Orientation="Horizontal" Margin="0,14,0,0"><Button Style="{StaticResource PrimaryButton}" Click="CreateInstance_Click" Content="Erstellen"/><Button Style="{StaticResource SecondaryButton}" Click="CancelCreate_Click" Margin="8,0,0,0" Content="Abbrechen"/></StackPanel>
                                </StackPanel>
                            </Border>
                            <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="24">
                                <StackPanel>
                                    <TextBlock Text="AUSGEWÄHLTE INSTANZ" FontSize="10" FontWeight="Bold" Foreground="{StaticResource MutedText}"/>
                                    <TextBlock x:Name="InstanceDetailName" Text="—" FontSize="26" FontWeight="Bold" Margin="0,8,0,2"/>
                                    <TextBlock x:Name="InstanceDetailMeta" Text="—" Foreground="{StaticResource SecondaryText}"/>
                                    <Border Height="1" Background="#282E3D" Margin="0,18"/>
                                    <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="Profil" FontSize="10" Foreground="{StaticResource MutedText}"/><TextBlock x:Name="InstanceDetailPreset" Text="Balanced" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel>
                                        <StackPanel Grid.Column="1"><TextBlock Text="Starts" FontSize="10" Foreground="{StaticResource MutedText}"/><TextBlock x:Name="InstanceDetailLaunches" Text="0" FontWeight="SemiBold" Margin="0,4,0,0"/></StackPanel>
                                    </Grid>
                                    <WrapPanel Margin="0,22,0,0">
                                        <Button Style="{StaticResource PrimaryButton}" Click="Play_Click" Content="▶ Spielen" Margin="0,0,8,8"/>
                                        <Button Style="{StaticResource SecondaryButton}" Click="OpenInstanceFolder_Click" Content="Ordner" Margin="0,0,8,8"/>
                                        <Button Style="{StaticResource SecondaryButton}" Click="DuplicateInstance_Click" Content="Duplizieren" Margin="0,0,8,8"/>
                                        <Button Style="{StaticResource DangerButton}" Click="DeleteInstance_Click" Content="Löschen" Margin="0,0,8,8"/>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Grid>

                <!-- LIBRARY -->
                <Grid x:Name="LibraryPage" Visibility="Collapsed" AllowDrop="True" DragOver="Library_DragOver" Drop="Library_Drop">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Bibliothek" FontSize="29" FontWeight="Bold"/><TextBlock x:Name="LibraryInstanceName" Text="Content für —" FontSize="13" Foreground="{StaticResource SecondaryText}" Margin="0,4,0,0"/></StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal"><Button Style="{StaticResource SecondaryButton}" Click="OpenContentFolder_Click" Content="Ordner öffnen"/><Button Style="{StaticResource PrimaryButton}" Click="AddContent_Click" Margin="8,0,0,0" Content="＋ Datei hinzufügen"/></StackPanel>
                    </Grid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,14">
                        <Button Style="{StaticResource SecondaryButton}" Tag="Mods" Click="LibraryKind_Click" Content="Mods" Margin="0,0,7,0"/>
                        <Button Style="{StaticResource SecondaryButton}" Tag="ResourcePacks" Click="LibraryKind_Click" Content="Resource Packs" Margin="0,0,7,0"/>
                        <Button Style="{StaticResource SecondaryButton}" Tag="ShaderPacks" Click="LibraryKind_Click" Content="Shaders"/>
                    </StackPanel>
                    <Grid Grid.Row="2"><Grid.RowDefinitions><RowDefinition Height="110"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                        <Border x:Name="LibraryDropZone" Background="#0D111A" BorderBrush="#3A4260" BorderThickness="1" CornerRadius="16" Margin="0,0,0,14">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><TextBlock Text="↓" FontSize="22" Foreground="#9983F5" HorizontalAlignment="Center"/><TextBlock x:Name="LibraryDropText" Text="JAR-Dateien hier ablegen" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,5,0,0"/><TextBlock Text="Dateien werden kopiert – deine Originale bleiben erhalten." FontSize="10" Foreground="{StaticResource MutedText}" HorizontalAlignment="Center" Margin="0,3,0,0"/></StackPanel>
                        </Border>
                        <Border Grid.Row="1" Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="14">
                            <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <ListBox x:Name="ContentList" Background="Transparent" BorderThickness="0" Foreground="White">
                                    <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,2"/><Setter Property="Background" Value="Transparent"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#242B40"/></Trigger></Style.Triggers></Style></ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate><DataTemplate><Grid Margin="10,9"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="100"/><ColumnDefinition Width="100"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Name}" FontWeight="SemiBold"/><TextBlock Text="{Binding SizeText}" FontSize="10" Foreground="{StaticResource MutedText}" Margin="0,2,0,0"/></StackPanel><TextBlock Grid.Column="1" Text="{Binding Kind}" Foreground="#8C96AB" VerticalAlignment="Center"/><TextBlock Grid.Column="2" Text="{Binding Status}" Foreground="#AFA3FF" VerticalAlignment="Center" HorizontalAlignment="Right"/></Grid></DataTemplate></ListBox.ItemTemplate>
                                </ListBox>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0"><Button Style="{StaticResource SecondaryButton}" Click="ToggleContent_Click" Content="Aktiv / Inaktiv"/><Button Style="{StaticResource DangerButton}" Click="DeleteContent_Click" Margin="8,0,0,0" Content="Entfernen"/></StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>

                <!-- LAB -->
                <ScrollViewer x:Name="LabPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="LUMINA Lab" FontSize="29" FontWeight="Bold"/>
                        <TextBlock Text="Features, die LUMINA mehr als nur zu einem Startknopf machen." FontSize="13" Foreground="{StaticResource SecondaryText}" Margin="0,4,0,20"/>
                        <UniformGrid Columns="3">
                            <Border Background="{StaticResource Card}" BorderBrush="#3D315E" BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,7,0"><StackPanel><TextBlock Text="◈  Safe Launch" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Sichert wichtige Configs vor jedem Start. Hilft, wenn ein Mod oder Setting deine Instanz zerlegt." TextWrapping="Wrap" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,8,0,15"/><CheckBox x:Name="SafeLaunchCheck" Content="Vor jedem Start sichern" Click="LabToggle_Click"/></StackPanel></Border>
                            <Border Background="{StaticResource Card}" BorderBrush="#3D315E" BorderThickness="1" CornerRadius="16" Padding="20" Margin="4,0"><StackPanel><TextBlock Text="⚡  Smart Memory" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="LUMINA erkennt deinen RAM und wählt automatisch einen vernünftigen Wert statt blind 16 GB zu vergeben." TextWrapping="Wrap" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,8,0,15"/><CheckBox x:Name="SmartMemoryCheck" Content="RAM automatisch wählen" Click="LabToggle_Click"/></StackPanel></Border>
                            <Border Background="{StaticResource Card}" BorderBrush="#3D315E" BorderThickness="1" CornerRadius="16" Padding="20" Margin="7,0,0,0"><StackPanel><TextBlock Text="◎  Focus Mode" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="Minimiert LUMINA automatisch, sobald das Spiel läuft, und bringt dich nach dem Beenden zurück." TextWrapping="Wrap" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,8,0,15"/><CheckBox x:Name="FocusModeCheck" Content="Beim Spielen fokussieren" Click="LabToggle_Click"/></StackPanel></Border>
                        </UniformGrid>

                        <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="22" Margin="0,16,0,0">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="340"/></Grid.ColumnDefinitions>
                                <StackPanel><TextBlock Text="Quick Connect" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="Starte die aktive Instanz direkt auf deinem Server. Beispiel: play-vertex.com oder host:25565" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap"/></StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"><TextBox x:Name="QuickServerText" Width="230"/><Button Style="{StaticResource PrimaryButton}" Click="SaveQuickServer_Click" Margin="8,0,0,0" Content="Speichern"/></StackPanel>
                            </Grid>
                        </Border>

                        <TextBlock Text="Performance Profile" FontSize="18" FontWeight="SemiBold" Margin="0,22,0,10"/>
                        <UniformGrid Columns="4">
                            <Border Background="#171C29" CornerRadius="14" Padding="16" Margin="0,0,6,0"><StackPanel><TextBlock Text="Competitive" FontWeight="SemiBold"/><TextBlock Text="Schnelle GC-Ziele, 6 GB" FontSize="10" Foreground="{StaticResource MutedText}" Margin="0,4,0,10"/><Button Style="{StaticResource SecondaryButton}" Tag="Competitive" Click="Preset_Click" Content="Aktivieren"/></StackPanel></Border>
                            <Border Background="#171C29" CornerRadius="14" Padding="16" Margin="2,0,4,0"><StackPanel><TextBlock Text="Balanced" FontWeight="SemiBold"/><TextBlock Text="Allround, 6 GB" FontSize="10" Foreground="{StaticResource MutedText}" Margin="0,4,0,10"/><Button Style="{StaticResource SecondaryButton}" Tag="Balanced" Click="Preset_Click" Content="Aktivieren"/></StackPanel></Border>
                            <Border Background="#171C29" CornerRadius="14" Padding="16" Margin="4,0,2,0"><StackPanel><TextBlock Text="Quality" FontWeight="SemiBold"/><TextBlock Text="Shader/Packs, 8 GB" FontSize="10" Foreground="{StaticResource MutedText}" Margin="0,4,0,10"/><Button Style="{StaticResource SecondaryButton}" Tag="Quality" Click="Preset_Click" Content="Aktivieren"/></StackPanel></Border>
                            <Border Background="#171C29" CornerRadius="14" Padding="16" Margin="6,0,0,0"><StackPanel><TextBlock Text="Low Spec" FontWeight="SemiBold"/><TextBlock Text="Leicht, 4 GB" FontSize="10" Foreground="{StaticResource MutedText}" Margin="0,4,0,10"/><Button Style="{StaticResource SecondaryButton}" Tag="Low Spec" Click="Preset_Click" Content="Aktivieren"/></StackPanel></Border>
                        </UniformGrid>
                    </StackPanel>
                </ScrollViewer>

                <!-- DOWNLOADS -->
                <StackPanel x:Name="DownloadsPage" Visibility="Collapsed">
                    <TextBlock Text="Downloads" FontSize="29" FontWeight="Bold"/>
                    <TextBlock Text="Minecraft, Libraries und Loader – direkt im Client sichtbar." FontSize="13" Foreground="{StaticResource SecondaryText}" Margin="0,4,0,20"/>
                    <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="22">
                        <StackPanel><TextBlock x:Name="DownloadStatus" Text="Keine aktiven Downloads" FontSize="15" FontWeight="SemiBold"/><ProgressBar x:Name="DownloadProgress" Value="0" Margin="0,13,0,0"/><TextBlock Text="LUMINA prüft nur Dateien, die für deine ausgewählte Instanz benötigt werden." FontSize="10" Foreground="{StaticResource MutedText}" Margin="0,9,0,0"/></StackPanel>
                    </Border>
                    <TextBlock Text="Aktivität" FontSize="17" FontWeight="SemiBold" Margin="0,22,0,10"/>
                    <Border Background="#0E121B" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="14" Padding="12" Height="310"><ListBox x:Name="DownloadLog" Background="Transparent" BorderThickness="0" Foreground="#9DA7BA" FontFamily="Consolas" FontSize="11"/></Border>
                </StackPanel>

                <!-- SETTINGS -->
                <ScrollViewer x:Name="SettingsPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        <TextBlock Text="Einstellungen" FontSize="29" FontWeight="Bold"/>
                        <TextBlock Text="Globale Start- und Client-Einstellungen." FontSize="13" Foreground="{StaticResource SecondaryText}" Margin="0,4,0,20"/>
                        <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="22">
                            <StackPanel>
                                <TextBlock Text="Speicher" FontSize="17" FontWeight="SemiBold"/><TextBlock Text="Zugewiesener RAM" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,14,0,7"/>
                                <Slider x:Name="RamSlider" Minimum="2048" Maximum="16384" TickFrequency="512" IsSnapToTickEnabled="True" ValueChanged="RamSlider_ValueChanged"/>
                                <TextBlock x:Name="RamValue" Text="6144 MB" Foreground="#B8ABFF" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <Border Height="1" Background="#272D3C" Margin="0,18"/>
                                <TextBlock Text="Fensterauflösung" FontSize="17" FontWeight="SemiBold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0"><TextBox x:Name="WidthText" Width="120"/><TextBlock Text="×" Margin="10,8" Foreground="{StaticResource MutedText}"/><TextBox x:Name="HeightText" Width="120"/></StackPanel>
                                <TextBlock Text="Java Override (optional)" FontSize="17" FontWeight="SemiBold" Margin="0,20,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0"><TextBox x:Name="JavaPathText" Width="520"/><Button Style="{StaticResource SecondaryButton}" Click="BrowseJava_Click" Margin="8,0,0,0" Content="Durchsuchen"/></StackPanel>
                                <CheckBox x:Name="CloseOnStartCheck" Content="LUMINA nach Spielstart schließen" Margin="0,18,0,0"/>
                                <Button Style="{StaticResource PrimaryButton}" Click="SaveSettings_Click" HorizontalAlignment="Left" Margin="0,20,0,0" Content="Einstellungen speichern"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <!-- ACCOUNT -->
                <ScrollViewer x:Name="AccountPage" Visibility="Collapsed">
                    <StackPanel MaxWidth="850" HorizontalAlignment="Left">
                        <TextBlock Text="Account" FontSize="29" FontWeight="Bold"/>
                        <TextBlock Text="Melde dich normal mit deinem Microsoft-/Minecraft-Account an." FontSize="13" Foreground="{StaticResource SecondaryText}" Margin="0,4,0,20"/>
                        <Border CornerRadius="20" Padding="28" BorderBrush="#413667" BorderThickness="1">
                            <Border.Background><LinearGradientBrush StartPoint="0,0" EndPoint="1,1"><GradientStop Color="#211A35"/><GradientStop Color="#121620" Offset="1"/></LinearGradientBrush></Border.Background>
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <Border Width="72" Height="72" CornerRadius="36" Background="#302653" VerticalAlignment="Center"><TextBlock Text="☺" FontSize="32" Foreground="#C9BCFF" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock x:Name="AccountState" Text="NICHT ANGEMELDET" FontSize="10" FontWeight="Bold" Foreground="#8876CE"/><TextBlock x:Name="AccountPlayerName" Text="Microsoft Account" FontSize="24" FontWeight="Bold" Margin="0,5,0,0"/><TextBlock x:Name="AccountUuid" Text="Melde dich an, um Minecraft Java zu starten." Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,4,0,0"/></StackPanel>
                                <StackPanel Grid.Column="2" VerticalAlignment="Center"><Button x:Name="LoginButton" Style="{StaticResource PrimaryButton}" Click="Login_Click" Content="Mit Microsoft anmelden"/><Button x:Name="LogoutButton" Style="{StaticResource SecondaryButton}" Click="Logout_Click" Content="Abmelden" Margin="0,8,0,0" Visibility="Collapsed"/></StackPanel>
                            </Grid>
                        </Border>
                        <Border Background="{StaticResource Card}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,16,0,0"><StackPanel><TextBlock Text="So funktioniert die Anmeldung" FontSize="16" FontWeight="SemiBold"/><TextBlock Text="LUMINA öffnet den offiziellen Microsoft-Anmeldefluss. Danach wird dein Minecraft-/Xbox-Login verwendet; beim nächsten Start versucht LUMINA dich automatisch wieder anzumelden." TextWrapping="Wrap" Foreground="{StaticResource SecondaryText}" FontSize="11" Margin="0,8,0,0"/></StackPanel></Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
