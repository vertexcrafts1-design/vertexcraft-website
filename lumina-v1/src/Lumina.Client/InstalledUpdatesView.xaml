<UserControl x:Class="Lumina.Client.InstalledUpdatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Installiert &amp; Updates" FontSize="17" FontWeight="SemiBold"/>
                <TextBlock Text="Modrinth-Installationen werden automatisch geprüft. Manuelle Dateien bleiben lokal und unangetastet." FontSize="10" Foreground="#748096" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Click="Refresh_Click" Content="↻  Neu prüfen"/>
        </Grid>

        <Grid Grid.Row="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ItemsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="180"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding FileName}" FontSize="9" Foreground="#687389" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding KindText}" FontSize="9" Foreground="#7C879D"/>
                                        <TextBlock Text="{Binding VersionText}" FontSize="10" Foreground="#B0B9CB" Margin="0,3,0,0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding StatusText}" FontSize="9" Foreground="#67D9AA" Margin="0,3,0,0"/>
                                    </StackPanel>
                                    <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Padding="13,8" MinWidth="112"
                                            IsEnabled="{Binding HasUpdate}" Tag="{Binding}" Click="Update_Click" Content="{Binding ActionText}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <StackPanel x:Name="EmptyState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="◇" FontSize="38" Foreground="#554A78" HorizontalAlignment="Center"/>
                <TextBlock Text="Noch kein Content in dieser Instanz." FontSize="12" Foreground="#7E899F" Margin="0,9,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
